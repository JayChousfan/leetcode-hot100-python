"""
============================================================
LeetCode 394. 字符串解码 (Decode String) — Hot 100
============================================================
难度：中等

按照 k[encoded_string] 表示括号内字符串重复 k 次的规则，返回解码结果。

示例：
  输入: s = "3[a2[c]]"
  输出: "accaccacc"

要求：
  - 函数名: decodeString
  - 参数: s，编码字符串，str
  - 返回值: 解码后的字符串，str
============================================================
"""

# TODO: 在这里写出完整的 def decodeString(...): 函数


# =============================================================================
# 测试（不要修改）
# =============================================================================
if __name__ == "__main__":
    assert decodeString("3[a2[c]]") == "accaccacc", "示例1失败"
    assert decodeString("3[a]2[bc]") == "aaabcbc", "并列编码测试失败"
    print("✅ P20 字符串解码 通过！")
