"""
============================================================
LeetCode 33. 搜索旋转排序数组 (Search in Rotated Sorted Array) — Hot 100
============================================================
难度：中等

在元素互不相同的旋转升序数组中查找 target，要求时间复杂度 O(log n)。

示例：
  输入: nums = [4,5,6,7,0,1,2], target = 0
  输出: 4

要求：
  - 函数名: search
  - 参数: nums 为 list[int]，target 为 int
  - 返回值: target 的下标，不存在返回 -1
============================================================
"""

# TODO: 在这里写出完整的 def search(...): 函数


# =============================================================================
# 测试（不要修改）
# =============================================================================
if __name__ == "__main__":
    assert search([4, 5, 6, 7, 0, 1, 2], 0) == 4, "示例1失败"
    assert search([4, 5, 6, 7, 0, 1, 2], 3) == -1, "目标不存在测试失败"
    print("✅ P18 搜索旋转排序数组 通过！")
